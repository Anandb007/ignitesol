apiVersion: v1
kind: Service

metadata:
  name: sample-test-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: sample-test

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
